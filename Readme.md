# Arch Linux setup

The install.sh script should be run on a fresh installation of arch linux.
If during the installation a newer kernel version than the one installed is available, errors could occur.
